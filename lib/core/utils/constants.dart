class AppConstants {
  static const String appName = 'Bigkas';
  static const String appTagline = 'Speech coach for everyone';
  
  // Animation durations
  static const Duration splashDuration = Duration(milliseconds: 300);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 150);
  
  // Padding and spacing
  static const double defaultPadding = 40.0;
  static const double smallSpacing = 8.0;
  static const double mediumSpacing = 16.0;
  static const double largeSpacing = 40.0;
  
  // Button dimensions
  static const double buttonHeight = 50.0;
  static const double buttonRadius = 25.0;
  
  // Icon sizes
  static const double largeIconSize = 120.0;
  static const double mediumIconSize = 80.0;
  
  // Page indicator
  static const double indicatorSize = 8.0;
}